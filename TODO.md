# 食序待办清单

最后更新：2026-09-05

## 待办：纯文本自动识别为菜谱

目标：用户粘贴一段自然语言菜谱、聊天记录或笔记后，由大模型识别并整理为可编辑的结构化菜谱，用户确认后再保存到菜谱库。

### 产品流程

- [ ] 在“菜谱”页面增加“文本识别”入口
- [ ] 提供大文本输入框，支持直接粘贴菜谱原文
- [ ] 点击“识别菜谱”后显示处理中状态，避免重复提交
- [ ] 展示结构化预览，不直接覆盖或保存用户数据
- [ ] 用户可以在预览页修改识别结果
- [ ] 用户确认后保存为普通菜谱，后续与手动创建的菜谱使用同一数据结构
- [ ] 识别失败、网络失败、内容为空时给出明确提示
- [ ] 支持一段文本中包含多个菜谱，并允许选择要保存的菜谱

### 识别结果

大模型需要尽量输出 JSON，而不是直接输出展示文本。目标字段包括：

- [ ] 菜谱名称
- [ ] 分类
- [ ] 烹饪方式
- [ ] 烹饪时间
- [ ] 难度
- [ ] 份量
- [ ] 标签
- [ ] 参考链接（如果原文包含）
- [ ] 食材总表：名称、用量、单位
- [ ] 烹饪步骤：步骤顺序、使用食材、处理方式、预计时长、步骤说明
- [ ] 原文片段或识别备注，方便用户核对不确定内容

### 识别规则

- [ ] 不确定的用量不能臆造，保留为空或标记为“适量”
- [ ] 同义食材尽量统一名称，但不能擅自改变用户原意
- [ ] “少许、适量、几片、两勺”等模糊用量必须保留原文表达
- [ ] 步骤中的食材应尽量关联到食材总表
- [ ] 原文只有做法、没有完整食材表时，也允许生成菜谱
- [ ] 原文包含多个菜名、分隔线或标题时，尝试拆分为多个菜谱
- [ ] 无法确认的字段放入识别备注，不阻止用户继续编辑

### 技术方案

- [ ] 抽象 `parseRecipeText(text)` 接口，前端只依赖统一的结构化结果
- [ ] 增加大模型适配层，先支持一种 OpenAI-compatible Chat Completions 接口
- [ ] 设计严格的 JSON Schema，并在客户端校验和修正默认值
- [ ] 处理模型返回 Markdown 代码块、额外说明文字和不完整 JSON
- [ ] 增加请求超时、重试和取消能力
- [ ] 不把 API Key 写入 GitHub 静态页面或公开前端代码
- [ ] 明确调用方式：本地代理服务、用户自带 API Key，或后续接入受控后端
- [ ] 在未配置大模型时保留手动创建菜谱功能，不影响现有应用

### 隐私与数据

- [ ] 在发送前明确提示：菜谱文本会发送到配置的大模型服务
- [ ] 默认不上传库存、采购金额、照片和其他无关本地数据
- [ ] 仅发送用户主动提交的文本和必要的解析指令
- [ ] 识别结果保存前仍由用户确认，避免错误内容直接进入菜谱库
- [ ] 导入/导出 JSON 时保留识别来源和备注（如果用户选择保留）

### 验收标准

- [ ] 粘贴一段包含菜名、食材和步骤的自然语言，可以生成可编辑预览
- [ ] 预览可以修改名称、食材、用量、步骤和分类
- [ ] 确认后生成的菜谱可以正常查看、编辑、安排到餐次
- [ ] 识别出的食材可继续用于库存扣减逻辑
- [ ] 失败时不产生半成品菜谱，不破坏已有数据
- [ ] 在没有 API 配置时，应用仍可正常启动和使用其他功能

## 后续可选增强

- [ ] 从剪贴板直接识别
- [ ] 从图片或截图 OCR 后再识别菜谱
- [ ] 从网页链接抓取正文后识别
- [ ] 批量整理历史菜谱笔记
- [ ] 根据用户确认结果积累个人食材和分类词典
